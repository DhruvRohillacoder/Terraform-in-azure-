trigger:
- main

pool: 
  name: dhruvagent

steps:
# Terraform Init (Local backend)
 - task: PowerShell@2
   inputs:
     filePath: '$(System.DefaultWorkingDirectory)/basic code'
     arguments: 'dir'