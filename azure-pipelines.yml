trigger: 
 branches:
   include:
     - main
     - feature01

pool: DEV

stages:
  -  stage: stagefirstt 
     displayName: start first stage
     jobs:
       - job: terraformintjob
         steps:
          - task: TerraformInstaller@1
            inputs:
              terraformVersion: 'latest'
